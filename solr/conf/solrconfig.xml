<?xml version="1.0" encoding="UTF-8" ?>
<config>
  <!-- NOTE: various comments and unused configuration possibilities have been purged
     from this file.  Please refer to http://wiki.apache.org/solr/SolrConfigXml,
     as well as the default solrconfig file included with Solr -->

  <abortOnConfigurationError>${solr.abortOnConfigurationError:true}</abortOnConfigurationError>

  <luceneMatchVersion>9.6.1</luceneMatchVersion>

  <directoryFactory name="DirectoryFactory" class="${solr.directoryFactory:solr.StandardDirectoryFactory}"/>

  <updateHandler class="solr.DirectUpdateHandler2">
    <updateLog>
      <str name="dir">${solr.core0.data.dir:}</str>
    </updateLog>
  </updateHandler>

  <!-- solr lib dirs -->
  <lib dir="${solr.install.dir:../../../..}/modules/analysis-extras/lib" />

  <dataDir>${solr.data.dir:}</dataDir>

  <jmx />

  <requestHandler name="search" class="solr.SearchHandler" default="true">
    <lst name="defaults">
      <str name="defType">edismax</str>
      <str name="echoParams">explicit</str>
      <str name="q.alt">*:*</str>

      <!-- keep your tuning -->
      <str name="mm">2&lt;-1 5&lt;-2 6&lt;90%</str>
      <int name="qs">1</int>
      <int name="ps">2</int>
      <float name="tie">0.01</float>

      <!-- Path 2: search ONLY the plain text field -->
      <str name="qf">ocrtext</str>
      <str name="pf">ocrtext^10</str>

      <!-- Return useful fields by default (optional if Rails sets fl) -->
      <str name="fl">id,canvas_index,ocrtext,ocrbbox,score</str>

      <!-- Highlight settings that fit sidecar bbox -->
      <str name="hl">true</str>
      <str name="hl.method">unified</str>
      <str name="hl.fl">ocrtext</str>
      <str name="hl.offsetSource">ANALYSIS</str>
      <int name="hl.snippets">10</int>
      <str name="hl.tag.pre">&lt;em&gt;</str>
      <str name="hl.tag.post">&lt;/em&gt;</str>
      <str name="hl.tag.ellipsis"> </str>
      <str name="hl.requireFieldMatch">true</str>

      <!-- keep your sort -->
      <str name="sort">canvas_index asc</str>
    </lst>
  </requestHandler>

  <!-- realtime get handler, guaranteed to return the latest stored fields
    of any document, without the need to commit or open a new searcher. The current
    implementation relies on the updateLog feature being enabled. -->
  <requestHandler name="/get" class="solr.RealTimeGetHandler">
    <lst name="defaults">
      <str name="omitHeader">true</str>
    </lst>
  </requestHandler>

  <requestHandler name="/replication" class="solr.ReplicationHandler" startup="lazy" />

  <requestDispatcher handleSelect="true">
    <requestParsers enableRemoteStreaming="true" multipartUploadLimitInKB="2048" />
  </requestDispatcher>

  <requestHandler name="/analysis/field" startup="lazy" class="solr.FieldAnalysisRequestHandler" />
</config>
